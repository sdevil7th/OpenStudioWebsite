These public metadata/appcast files were downloaded unchanged from the
`sdevil7th/OpenStudio` GitHub release `v0.1.01` on September 9, 2026.
They exercise website-first deployment with the currently published schema-4
runtime (Windows base/install plans, macOS arm64, Linux x64/CUDA/ROCm).
They are test fixtures only and must never be copied to production by default.
